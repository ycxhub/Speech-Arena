Share the copy-pastebale SQL code that I should use on Supabase's SQL editor

Also, share instruction on whether I should Paste and run the full script at once or one after the other.

If I have run the script one after the other, share the order in which I should run the script. 

Please take into account if any of other code changes needs to deployed before running the SQL script. 