/**
 * TODO: This file will be auto-generated from the Supabase schema in PRD 03.
 * Run: npm run db:types
 * Before first run: replace <project-id> in the db:types script in package.json
 * with your Supabase project ID (Dashboard → Settings → General).
 */
export type Database = Record<string, never>;
